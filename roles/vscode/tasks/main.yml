- name: Install code
  aur:
    name: visual-studio-code-bin
    hash: 995225efeb955613f6e41ffe5526e39cd077faa2
    user: "{{ user.name }}"

- name: Installing extensions
  command: /usr/bin/code --install-extension {{ item }}
  with_items: "{{ vscode_extensions }}"
  become: yes
  become_user: "{{ user.name }}"
